function promedio() {
    let numero = parseInt(document.getElementById('numero').value);
    const arrayNumeros = [];

    document.getElementById('resultado').innerText = `La cantidad de números pares entre ${inicio} y ${fin} es: ${contador}`;
}